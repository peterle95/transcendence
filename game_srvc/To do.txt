1. Pick one auth contract and use it everywhere
Right now game_srvc expects a Bearer verification endpoint at 
auth.ts:26, but auth_srvc currently exposes NextAuth catch-all 
at auth_srvc/app/api/auth/[...nextauth]/route.ts.

Do one of these:

Add a dedicated auth_srvc verify/introspection endpoint (recommended for service-to-service).
Or change game_srvc auth to validate via existing auth_srvc session endpoints (for example users/me).
Without this, services are not truly connected.
DONE

2. Fix the requireAuth contract mismatch in game_srvc
In auth.ts:112, requireAuth returns authenticated/user/status.
But route.ts:30 expects user/response.
This must be aligned first, otherwise auth checks are unreliable.
DONE

3. Make frontend requests carry auth proof consistently
Your save call in ranking.js:53 currently sends only JSON body.
If using cookie session, ensure requests are sent with credentials policy consistently.
If using bearer flow, ensure frontend/BFF injects Authorization header consistently.
DONE

4. Stop trusting user identity coming from client payload
In route.ts:68, playerId comes from request body.
This is unsafe if not validated against authenticated identity/session context.
Server must derive/validate who can write what.
DONE

5. Clean duplicated stale logic in stats save route
There is leftover duplicate code after the first return in route.ts:117.
Remove it to avoid confusion and accidental regressions.
DONE

6. Decide endpoint policy
Keep global ranking public at route.ts:1, or protect it if needed.
Add/secure personal stats endpoint (must require auth).
DONE

7. Confirm schema and identity linkage
Your DB model is already suitable in schema.prisma:

User table keyed by auth user id.
PlayerGameStats and GlobalLeaderboard linked to User.
So data model is fine; integration/auth flow is the missing piece.
8. End-to-end test checklist

Login once via frontend.
Call save stats from game flow: should pass only when authenticated.
Tamper payload userId in browser: server should reject/ignore.
Fetch ranking and stats: correct user attribution.
Logout: protected endpoints return 401.
If you want, next I can give you a concrete implementation 
plan with exact payload and response contracts for the auth 
verification endpoint so both services match on first try.